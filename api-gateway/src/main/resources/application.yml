server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: product-workflow
          uri: http://localhost:8083
          predicates:
            - Path=/api/product-workflow/**

